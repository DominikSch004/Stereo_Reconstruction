%YAML:1.0
# Pipeline step backend configuration.
# Used by StereoReconstruction and IcpFusion (pass a different path as first CLI argument,
# default is ../config.yaml relative to the build directory).
#
# Steps without an alternative implementation are not configurable:
#   - essential matrix / pose recovery (always cv::findEssentialMat + cv::recoverPose;
#     see the comment in Pipeline.cpp step 3 for why E-from-F was abandoned)

# select dataset and image pairs
dataset_id: 6
image_left_id: 35
image_right_id: 36
illumination_id: 3

# Image pairs fused by the IcpFusion executable
icp_view_range: [30, 40]

# sift | orb
feature_detector: sift

# select ratio threshold for FLANN after SIFT.
ratio_threshold: 0.75

# 42 | -1 for true randomness
rng_seed: -1

# opencv | custom | custom_magsac | custom_prosac
fundamental_matrix: custom_magsac

# opencv | custom  (custom = Loop & Zhang projective rectification)
rectification: custom

# opencv | custom  (custom = simplified semi-global matching (Hirschmüller 2008),
# Birchfield-Tomasi cost + 16-direction path aggregation)
disparity: custom

# Confidence cue used by confidence-weighted ICP. The hard left/right check is
# common to every ablation variant; the surviving soft confidence is c_stereo.
# Booleans use 0/1 for OpenCV FileStorage portability.
stereo_confidence_filter: 1
stereo_lr_max_diff: 1.5
stereo_photometric_scale: 25.0

# w = c_global * c_depth * c_edge * c_stereo. Disable terms here for production
# experiments; benchmark/ConfidenceIcpAblation.cpp composes every ablation from
# one fixed point cloud, so its comparisons never change the sampled geometry.
conf_use_global: 1
conf_use_depth: 1
conf_use_edge: 1
conf_use_stereo: 1

# opencv | custom  (custom = per-pixel DLT triangulation)
triangulation: custom

# point_to_point | point_to_plane  (only used by the IcpFusion executable)
# point_to_point is the default: the per-point normals available here come from
# +/-1px central differences on raw stereo disparity and are too noisy to
# support point-to-plane residuals (they caused tangential drift in fusion).
icp_mode: point_to_plane

# true | false non-linear refinement of (R, t) directly on the essential-matrix 
# space after recoverPose
pose_refinement: true

# (0, 1.0]  image downscale factor applied before every pipeline step, via
# Pipeline::preprocessScale. 1.0 = full resolution, no downscale.
processing_scale: 0.5

# Custom disparity backend only: toggles/thresholds for Sec 2.5, "Disparity
# Refinement" (Hirschmuller 2008): postprocessing layered on top of the core
# disparity algorithm (Sec 2.1-2.3: pixelwise cost, path aggregation, WTA +
# subpixel selection, and L/R consistency check). Each refinement stage below
# can be turned off to inspect its individual effect. Threshold values are given
# at full resolution (scale=1.0) and are rescaled internally by processing_scale.

# true | false  Sec 2.5.1: removes small isolated patches of wrong disparity
# ("peaks"). min_peak_segment_px isn't specified in the paper as it depends on
# the scene structure. We use 100 here to match StereoSGBM's speckleWindowSize.
# Raising it trades coverage for dense-vs-sparse accuracy.
disparity_peak_filtering: true
disparity_min_peak_segment_px: 100

# true | false  Sec 2.5.3: pushes coverage to 100% by interpolating remaining gaps.
# Trades some dense-vs-sparse accuracy for coverage -- should be on for scan6, where
# the denser result looks better.
disparity_gap_fill: true
